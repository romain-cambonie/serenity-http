{
  "extends": "./tsconfig.16.esm.json",
  "compilerOptions": {
    "importsNotUsedAsValues": "preserve" /* "preserve" because type / value injection needed for jest.expect.toThrow cause problems otherwise */
  },
  "include": ["../src/**/*.test.ts"],
  "exclude": ["../lib"]
}
